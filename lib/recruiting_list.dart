import 'package:flutter/material.dart';

class RecruitingListPage extends StatelessWidget {
  const RecruitingListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
